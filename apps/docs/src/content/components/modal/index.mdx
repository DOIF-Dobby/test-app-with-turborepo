---
title: 'Modal'
description: 'Modal'
date: '2025-08-29'
---

import { CodeBlock } from '@/components/code-block/code-block'

import Default from './default'
import DefaultRaw from './default?raw'
import Sizes from './sizes'
import SizesRaw from './sizes?raw'
import NonDismissible from './non-dismissible'
import NonDismissibleRaw from './non-dismissible?raw'
import DisableAnimation from './disable-animation'
import DisableAnimationRaw from './disable-animation?raw'
import MotionProps from './motion-props'
import MotionPropsRaw from './motion-props?raw'

# Modal

## Default

<CodeBlock preview={<Default />} code={DefaultRaw} />

## Sizes

<CodeBlock preview={<Sizes />} code={SizesRaw} />

## Non-dismissible

기본적으로 모달 영역 밖을 클릭하거나 `ESC` 키를 누르면 모달 닫힙니다. `isPointerDismissible` 옵션과 `isKeyboardDismissible` 옵션으로 제어할 수 있습니다.

<CodeBlock preview={<NonDismissible />} code={NonDismissibleRaw} />

## Disable Animation

<CodeBlock preview={<DisableAnimation />} code={DisableAnimationRaw} />

## Motion Props

`motionProps` 속성을 이용하여 애니메이션을 설정할 수 있습니다.

<CodeBlock preview={<MotionProps />} code={MotionPropsRaw} />
